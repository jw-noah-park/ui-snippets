import styles from './NeonBoxButton.module.css';

export default function NeonBoxButton() {
  return (
    <div className={styles.wrapper}>
      <button className={styles.neonBox}>MODULE CSS</button>
    </div>
  );
}
